# Phased Development Strategy (단계별 개발 전략)

> **Backend 먼저, Frontend 나중에 - 점진적 전환**

---

## 📋 목차

1. [개요](#개요)
2. [Phase 1: Backend Only](#phase-1-backend-only)
3. [Phase 2: Frontend](#phase-2-frontend)
4. [병행 운영](#병행-운영)

---

## 개요

### 왜 단계별 개발인가?

```
┌─────────────────────────────────────────────────────────────┐
│              단계별 개발의 장점                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 리스크 분산                                              │
│     • Backend 먼저 검증                                      │
│     • 안정화 후 Frontend                                     │
│     • 실패 시 롤백 쉬움                                      │
│                                                             │
│  2. 빠른 가치 전달                                           │
│     • Backend만으로도 자동매매 가능                          │
│     • API로 다양한 클라이언트 지원                           │
│     • 즉시 사용 가능                                         │
│                                                             │
│  3. 유연성                                                   │
│     • Backend 변경 없이 Frontend 교체 가능                   │
│     • 여러 Frontend 동시 지원 (Web, Mobile, CLI)            │
│                                                             │
│  4. 팀 분업                                                  │
│     • Backend 개발자 vs Frontend 개발자                      │
│     • 병렬 작업 가능 (나중에)                                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## Phase 1: Backend Only

### 기간: 1-2개월

### 목표
- FastAPI Backend 완성
- 모든 매매 로직 API화
- Frontend 없이도 완전 작동

### 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│              Phase 1: Backend Only                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  [사용자 인터페이스 - 4가지 선택]                            │
│                                                             │
│  1. Swagger UI (추천)                                        │
│     http://localhost:8000/docs                              │
│     • 자동 생성                                              │
│     • API 테스트                                             │
│     • 실시간 문서                                            │
│                                                             │
│  2. Streamlit (기존)                                         │
│     http://localhost:8501                                   │
│     • 기존 UI 유지                                           │
│     • FastAPI로 데이터 조회                                  │
│     • 빠른 프로토타이핑                                      │
│                                                             │
│  3. CLI (새로 만들기)                                        │
│     $ aegis buy 005930 1000000                              │
│     $ aegis portfolio                                       │
│     • 빠름                                                   │
│     • 자동화 쉬움                                            │
│                                                             │
│  4. Claude Code (현재 방식)                                  │
│     "오늘 거래 내역 보여줘"                                   │
│     • 자연어 대화                                            │
│     • 스크립트 자동 실행                                     │
│                                                             │
│           ↓ HTTP/REST                                       │
│                                                             │
│  ┌──────────────────────────────────┐                       │
│  │     FastAPI Backend              │                       │
│  │  ────────────────────────────    │                       │
│  │  • REST API                      │                       │
│  │  • WebSocket                     │                       │
│  │  • Business Logic                │                       │
│  │  • AI Integration                │                       │
│  └──────────────┬───────────────────┘                       │
│                 ↓                                           │
│  ┌──────────────────────────────────┐                       │
│  │      PostgreSQL + TimescaleDB    │                       │
│  └──────────────────────────────────┘                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Week-by-Week 계획

#### Week 1-2: FastAPI 기본 구조

```python
# app/main.py
from fastapi import FastAPI

app = FastAPI(title="AEGIS v3.0 API")

@app.get("/")
def root():
    return {"message": "AEGIS v3.0 Backend"}

@app.get("/health")
def health():
    return {"status": "healthy"}
```

**목표:**
- [ ] FastAPI 프로젝트 생성
- [ ] Database 연결
- [ ] 기본 CRUD API
- [ ] Swagger UI 확인

**테스트:** `http://localhost:8000/docs`

#### Week 3-4: 핵심 기능 API

```python
# API 엔드포인트

POST /api/orders/buy          # 매수
POST /api/orders/sell         # 매도
GET  /api/portfolio           # 포트폴리오 조회
GET  /api/trades/today        # 오늘 거래 내역
GET  /api/analysis/stock/{code}  # 종목 분석
```

**사용:**
```bash
# Swagger UI에서 테스트
# 또는 curl
curl http://localhost:8000/api/portfolio
```

#### Week 5-6: 고급 기능

```python
# WebSocket
WS /api/ws/realtime           # 실시간 데이터

# AI 분석
POST /api/ai/analyze          # AI 분석 실행
GET  /api/ai/recommendations  # AI 추천 종목

# 피라미딩
POST /api/pyramid/check       # 피라미딩 체크
POST /api/pyramid/execute     # 피라미딩 실행
```

#### Week 7-8: 최적화

```python
# 학습 시스템
GET  /api/learning/stats      # 학습 통계
POST /api/learning/optimize   # 최적화 실행

# SAFE MODE
GET  /api/system/mode         # 시스템 모드
POST /api/system/mode/reset   # 수동 리셋
```

---

## Phase 2: Frontend

### 기간: 2-3개월 (Phase 1 완료 후)

### 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│              Phase 2: Full Stack                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────────────────┐                       │
│  │     Next.js Frontend             │  ← 신규 개발          │
│  │  ────────────────────────────    │                       │
│  │  • React 18                      │                       │
│  │  • TailwindCSS                   │                       │
│  │  • TanStack Query                │                       │
│  │  • shadcn/ui                     │                       │
│  └──────────────┬───────────────────┘                       │
│                 ↓ HTTP/WebSocket                            │
│  ┌──────────────────────────────────┐                       │
│  │     FastAPI Backend              │  ← 이미 완성!          │
│  │  (Phase 1에서 완성됨)             │                       │
│  └──────────────┬───────────────────┘                       │
│                 ↓                                           │
│  ┌──────────────────────────────────┐                       │
│  │      PostgreSQL                  │                       │
│  └──────────────────────────────────┘                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 병행 운영

### Phase 1 동안 사용 방법

#### 1. Swagger UI (API 테스트)

```
http://localhost:8000/docs

✅ 모든 API 테스트 가능
✅ 자동 문서화
✅ 개발 불필요
```

#### 2. Streamlit (현재 대시보드)

```python
# dashboard/app_v3.py (수정)

import requests

# 기존: DB 직접 조회
# db.query(Portfolio).all()

# 변경: FastAPI 호출
response = requests.get('http://localhost:8000/api/portfolio')
portfolio = response.json()
```

**장점:**
- 기존 UI 그대로 사용
- Backend만 교체
- 빠른 전환

#### 3. CLI 스크립트

```python
# scripts/cli.py

import typer
import requests

app = typer.Typer()

@app.command()
def buy(stock_code: str, amount: int):
    """매수"""
    response = requests.post(
        'http://localhost:8000/api/orders/buy',
        json={'stock_code': stock_code, 'amount': amount}
    )
    print(response.json())

@app.command()
def portfolio():
    """포트폴리오 조회"""
    response = requests.get('http://localhost:8000/api/portfolio')
    data = response.json()

    for stock in data['holdings']:
        print(f"{stock['name']}: {stock['quantity']}주")

if __name__ == '__main__':
    app()
```

**사용:**
```bash
python scripts/cli.py buy 005930 1000000
python scripts/cli.py portfolio
```

#### 4. Claude Code (지금처럼)

```python
# scripts/aegis_helper.py

def get_today_trades():
    """오늘 거래 내역 (FastAPI 호출)"""
    import requests

    response = requests.get('http://localhost:8000/api/trades/today')
    return response.json()

# Claude Code에서
"오늘 거래 내역 보여줘"
→ python scripts/aegis_helper.py trades
```

---

## 📊 비교표

| 방법 | 개발 필요 | 속도 | 편의성 | 추천도 |
|-----|----------|------|--------|--------|
| **Swagger UI** | ❌ (자동) | ⚡ | ⭐⭐⭐ | 🔥🔥🔥 |
| **Streamlit** | ✅ (소폭) | ⚡ | ⭐⭐⭐⭐ | 🔥🔥 |
| **CLI** | ✅ (중간) | ⚡⚡ | ⭐⭐ | 🔥 |
| **Claude Code** | ✅ (간단) | ⚡ | ⭐⭐⭐⭐⭐ | 🔥🔥🔥 |

---

## 💡 추천 조합

### Phase 1 동안

```
1. 개발: Swagger UI (API 테스트)
2. 모니터링: Streamlit (기존 UI)
3. 자동화: CLI 스크립트
4. 대화: Claude Code
```

**예시 하루:**
```
06:00 - Scheduler 자동 실행 (Backend)
09:00 - Streamlit으로 모니터링
10:00 - Claude Code로 "오늘 종목 추천해줘"
15:00 - Swagger UI로 수동 거래 테스트
23:00 - CLI로 일일 리포트 생성
```

---

## 🎯 결론

### ✅ 가능합니다!

**Backend만으로:**
- ✅ 자동매매 100% 가능
- ✅ 모니터링 가능 (Streamlit)
- ✅ Claude Code 사용 가능
- ✅ API 테스트 가능 (Swagger)

**Frontend는:**
- ⏳ 천천히 개발
- ⏳ 완성도 높게
- ⏳ Backend 안정화 후

**오히려 이 방식이 더 안전하고 효율적입니다!** 🚀

---

**작성일**: 2025-12-09
**작성자**: wonny
**버전**: 1.0.0
